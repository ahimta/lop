pre-commit:
  parallel: false
  commands:
    continuous-integration:
      run: |
        trap ./scripts/notify-user.sh EXIT
        podman build --tag lop --file ./Containerfile .
        podman run --rm lop
